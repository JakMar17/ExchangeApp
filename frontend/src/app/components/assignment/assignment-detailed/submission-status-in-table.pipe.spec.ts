import { SubmissionStatusInTablePipe } from './submission-status-in-table.pipe';

describe('SubmissionStatusInTablePipe', () => {
  it('create an instance', () => {
    const pipe = new SubmissionStatusInTablePipe();
    expect(pipe).toBeTruthy();
  });
});
