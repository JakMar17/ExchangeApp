import { AssignmentStatusPipe } from './assignment-status.pipe';

describe('AssignmentStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new AssignmentStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
